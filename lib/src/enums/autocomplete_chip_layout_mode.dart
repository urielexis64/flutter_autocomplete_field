/// Defines how selected chips are laid out in multiple-selection fields.
enum AutocompleteChipLayoutMode {
  /// Renders chips using a multi-line [Wrap] that can grow vertically.
  wrap,

  /// Renders chips in a single horizontal row with horizontal scrolling.
  horizontalScroll,
}
